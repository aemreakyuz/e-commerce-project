const initialState = {
  productList: [],
  totalProductCount: "",
  pageCount: 0,
  activePage: "",
  fetchStates: "",
};

const productReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default productReducer;
